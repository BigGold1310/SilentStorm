---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clustersilences.silentstorm.biggold1310.ch
spec:
  group: silentstorm.biggold1310.ch
  names:
    kind: ClusterSilence
    listKind: ClusterSilenceList
    plural: clustersilences
    singular: clustersilence
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterSilence is the Schema for the clustersilences API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSilenceSpec defines the desired state of ClusterSilence
            properties:
              comment:
                type: string
              creator:
                type: string
              matchers:
                items:
                  properties:
                    isEqual:
                      default: true
                      description: isEqual allows to switch the matcher between equality
                        and non-equality. Default is equality (true)
                      type: boolean
                    isRegex:
                      default: false
                      description: isRegex allows to switch the matcher between regex
                        and non-regex. Default is false.
                      type: boolean
                    name:
                      description: name, within alertmanager usually known as label
                        name/key.
                      type: string
                    value:
                      description: value, within alertmanager usually known as label
                        value/value.
                      type: string
                  required:
                  - isRegex
                  - name
                  - value
                  type: object
                type: array
            required:
            - matchers
            type: object
          status:
            description: ClusterSilenceStatus defines the observed state of ClusterSilence
            properties:
              silenceID:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
